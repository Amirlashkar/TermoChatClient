pub enum Screen {
    Main,
    UserForm,
    RoomForm,
}

pub enum UserForm {
    SignUp,
    SignIn,
}

pub enum RoomForm {
    RoomCreator,
    RoomEdit,
}

pub enum Block {
    Rooms,
    Chat,
    Typing,
}
