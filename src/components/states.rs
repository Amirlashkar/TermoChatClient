pub enum Screen {
    Main,
    UserForm,
    RoomForm,
}

pub enum Forms {
    SignUp,
    SignIn,
    RoomCreator,
    RoomEdit,
    Typing,
}

pub enum Block {
    Rooms,
    Chat,
    Typing,
}

pub enum Modes {
    Normal,
    Insert,
}
